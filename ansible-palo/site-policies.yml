---
- hosts: localhost
  connection: local

  roles:
    - role: paloaltonetworks.paloaltonetworks
    
  tasks:
    - name: Creat policies
      script: python scripts/site-policies.py {{ username }} '{{ palo_password }}' {{ panorama }}
